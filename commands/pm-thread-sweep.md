---
description: Review, finish, decide, and safely close open project threads.
argument-hint: "[project, batch, or thread scope]"
---

# PM Thread Sweep

The user invoked this command with: $ARGUMENTS

1. Read and follow `/Users/preston/.codex/skills/pm-thread-sweep/SKILL.md`.
2. Treat `$ARGUMENTS` as the project, batch, or explicit thread scope.
3. If arguments are empty, infer the project only when the conversation makes
   one project unambiguous; otherwise ask for the missing scope.
4. Review evidence before closure, use autonomous feature build for safe
   unfinished work, and stage Preston decisions as numbered a/b/c choices.
5. Never close the current PM thread unless it is explicitly included and passes
   the same absolute closeability gate.
