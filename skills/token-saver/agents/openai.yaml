interface:
  display_name: "Token Saver"
  short_description: "Reduce context and retry token waste"
  default_prompt: "Use $token-saver to complete this task with the smallest useful context and output."
policy:
  allow_implicit_invocation: true
