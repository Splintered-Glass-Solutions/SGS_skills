interface:
  display_name: "Test"
  short_description: "Test the features changed in this task"
  default_prompt: "Use $test to validate only the feature or features worked on recently in this task."
policy:
  allow_implicit_invocation: false
