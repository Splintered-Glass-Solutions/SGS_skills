interface:
  display_name: "Full Suite Tests"
  short_description: "Run and persist complete environment QA suites"
  default_prompt: "Use $full-suite-tests with mandatory $token-saver enforcement to run the complete environment suite and persist every QA/QC result."
