interface:
  display_name: "Full Suite Tests"
  short_description: "Run a repo's broad test and QA suite for an environment"
  default_prompt: "Use $full-suite-tests to run or plan the repository full validation suite for the requested environment."
policy:
  allow_implicit_invocation: true
