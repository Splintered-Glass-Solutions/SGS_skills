interface:
  display_name: "Continue"
  short_description: "Resume paused Codex work cleanly"
  default_prompt: "Use $continue to resume the interrupted work from the current context."
policy:
  allow_implicit_invocation: true
