interface:
  display_name: "Bonfire Auth & Orgs"
  short_description: "Auth, org membership, and roles in Bonfire"
  default_prompt: "Use $bonfire-auth-org-permissions to adjust a Bonfire org access flow safely."
policy:
  allow_implicit_invocation: true
