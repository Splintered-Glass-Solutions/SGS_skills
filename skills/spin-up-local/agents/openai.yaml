interface:
  display_name: "Spin Up Local"
  short_description: "Run local app with active worktree changes"
  default_prompt: "Use $spin-up-local to run the current app locally for testing."
policy:
  allow_implicit_invocation: true
