interface:
  display_name: "Autonomous Feature Build"
  short_description: "Build and checkpoint full features end to end"
  default_prompt: "Use $autonomous-feature-build with mandatory $token-saver enforcement to orchestrate, checkpoint, build, and verify the feature end to end."

policy:
  allow_implicit_invocation: true
