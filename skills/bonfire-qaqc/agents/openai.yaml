interface:
  display_name: "Bonfire QA/QC"
  short_description: "Run Bonfire full-suite QAQC, page-by-page browser checks, smoke tests, and release checks"
  default_prompt: "Use $bonfire-qaqc to run a full Bonfire QAQC pass across deterministic tests and page-by-page Computer Use coverage, then record the results."
policy:
  allow_implicit_invocation: true
