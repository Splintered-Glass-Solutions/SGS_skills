interface:
  display_name: "Bonfire QA/QC"
  short_description: "Run and persist holistic Bonfire QA/QC sessions"
  default_prompt: "Use $bonfire-qaqc to run an evidence-backed Bonfire QA/QC session and persist the results in the live QA dashboard."
policy:
  allow_implicit_invocation: true
