interface:
  display_name: "Orchestrator Mode"
  short_description: "Use subagents while Codex orchestrates"
  default_prompt: "Use $orchestrator-mode with mandatory $token-saver enforcement, $codex-safe-run guardrails, and bounded cheaper capable subagents for independent slices."

policy:
  allow_implicit_invocation: true
