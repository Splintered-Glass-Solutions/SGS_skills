interface:
  display_name: "Local Dev Consolidation"
  short_description: "Consolidate and test a local dev-ready branch"
  default_prompt: "Use $local-dev-consolidation to merge local work into an isolated dev-ready branch, validate it locally, and stop before any push or deployment."
