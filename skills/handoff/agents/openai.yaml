interface:
  display_name: "Handoff"
  short_description: "Prepare a clean session handoff document"
  default_prompt: "Use $handoff to summarize this session into a handoff document for the next agent."
