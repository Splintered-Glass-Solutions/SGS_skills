interface:
  display_name: "Bonfire API Routes"
  short_description: "Next.js API route patterns for Bonfire"
  default_prompt: "Use $bonfire-api-routes to add a Bonfire API route with validation, auth, and tests."
policy:
  allow_implicit_invocation: true
