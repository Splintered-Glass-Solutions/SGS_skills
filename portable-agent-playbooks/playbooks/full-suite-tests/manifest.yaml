---
name: full-suite-tests
display_name: Full Suite Tests
description: Run or plan a repository's complete environment-scoped test and QA suite,
  including its durable QA/QC catalog, and persist per-check results to the configured
  QA dashboard/database or local run store. Always invoke and enforce $token-saver
  for these normally autonomous, log-heavy runs. Use when the user says "full-suite-tests
  in local", "full-suite-tests in dev", "full-suite-tests in prod", "run full suite
  in local/dev/prod", "full QA in dev", "test everything in prod", or similar full
  validation requests across any project.
source: skills/full-suite-tests/SKILL.md
entrypoint: AGENT.md
target_platforms:
- claude
- claude-code
- cursor
- openai-agents
- generic-agent
safety_level: approval-gated for external or irreversible actions
