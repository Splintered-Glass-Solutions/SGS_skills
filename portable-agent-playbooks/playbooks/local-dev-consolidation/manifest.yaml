---
name: local-dev-consolidation
display_name: Local Dev Consolidation
description: 'Consolidate work across local repos, branches, or worktrees into a clean
  local branch based on the confirmed dev baseline, resolve conflicts, validate locally,
  and prepare a local-testing handoff. This skill is local-only by default: it does
  not push or deploy unless the user separately and explicitly requests deployment
  to dev. Trigger on consolidate local work, clean up working trees, merge everything
  into a local dev branch, wrap this into a dev-ready branch, or get my local environment
  clean.'
source: skills/local-dev-consolidation/SKILL.md
entrypoint: AGENT.md
target_platforms:
- claude
- claude-code
- cursor
- openai-agents
- generic-agent
safety_level: approval-gated for external or irreversible actions
